<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Runtime-requested where required (API 33+ for notifications; location,
         camera and bluetooth scan are requested on demand by NativeModules). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Bluetooth scan/connect on API 31+; older devices fall back to location. -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!-- E8 platform + system native. VIBRATE → HapticsModule; WAKE_LOCK →
         keep-awake (FLAG_KEEP_SCREEN_ON also needs no perm, but WorkManager +
         WakeLock-style work want it); ACCESS_NETWORK_STATE → ConnectivityModule;
         USE_BIOMETRIC (+ legacy USE_FINGERPRINT) → BiometricsModule;
         RECEIVE_BOOT_COMPLETED → WorkManager re-schedules periodic work after a
         reboot. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-feature android:name="android.hardware.camera.any" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <uses-feature android:name="android.hardware.location" android:required="false" />

    <!-- Lets ACTION_VIEW to wa.me / the WhatsApp package resolve on API 30+. -->
    <queries>
        <package android:name="com.whatsapp" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:label="${appLabel}"
        android:allowBackup="false"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:extractNativeLibs="false">
        <!-- extractNativeLibs=false → .so stored uncompressed + page-aligned,
             opened directly from the APK (paired with packaging.jniLibs). -->

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- E8 FCM service (PushModule). The host reads the registration token
             via FirebaseMessaging.getInstance().token; receiving pushes needs this
             service. It is DECLARED but device-gated: FCM requires a Firebase
             project + a `google-services.json` at the module root + the
             google-services Gradle plugin. None of those are wired here (to keep
             the host buildable without a Firebase account), so at runtime
             FirebaseApp init fails and PushModule replies error="not_configured".
             Configuring Firebase is a documented device pendency. -->
        <service
            android:name=".TempestMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Exposes app-private capture files to the camera app (CameraModule). -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
