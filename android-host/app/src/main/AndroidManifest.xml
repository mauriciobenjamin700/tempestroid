<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- LEAN BASE manifest (feature-gating, F4). Only permissions for the
         always-present core capabilities live here. The heavy-feature entries —
         CAMERA/RECORD_AUDIO + the FileProvider (camera/qr) and the FCM <service>
         (push) — are injected by the manifest generator task in build.gradle.kts
         ONLY when the matching feature is in -Ptempest.features, so the lean APK
         never references a class from an absent dependency. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Bluetooth scan/connect on API 31+; older devices fall back to location. -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!-- E8 platform + system native. VIBRATE → HapticsModule; WAKE_LOCK →
         keep-awake (FLAG_KEEP_SCREEN_ON also needs no perm, but WorkManager +
         WakeLock-style work want it); ACCESS_NETWORK_STATE → ConnectivityModule;
         USE_BIOMETRIC (+ legacy USE_FINGERPRINT) → BiometricsModule;
         RECEIVE_BOOT_COMPLETED → WorkManager re-schedules periodic work after a
         reboot. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <uses-feature android:name="android.hardware.location" android:required="false" />

    <!-- Lets ACTION_VIEW to wa.me / the WhatsApp package resolve on API 30+. -->
    <queries>
        <package android:name="com.whatsapp" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:label="${appLabel}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:extractNativeLibs="false">
        <!-- extractNativeLibs=false → .so stored uncompressed + page-aligned,
             opened directly from the APK (paired with packaging.jniLibs). -->

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- TEMPEST_FEATURE_APPLICATION_ENTRIES: the manifest generator injects
             the camera FileProvider + the FCM <service> here when their feature
             is active. Do not remove this marker comment. -->
    </application>
</manifest>
